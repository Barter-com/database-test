@model IEnumerable<database_test.Models.item_table>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.trader_index.firstname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.trader_index.firstname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.itemname }) |
            @Html.ActionLink("Details", "Details", new { id=item.itemname }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.itemname })
        </td>
    </tr>
}

</table>
